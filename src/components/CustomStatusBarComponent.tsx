import { StyleSheet, StatusBar } from 'react-native'
import React from 'react'

export default function CustomStatusBarComponent() : React.JSX.Element {

  return (
    <StatusBar backgroundColor={'black'}/>
  )
}

const styles = StyleSheet.create({})