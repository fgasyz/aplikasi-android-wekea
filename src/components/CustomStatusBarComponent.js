import { StyleSheet, StatusBar } from 'react-native'
import React from 'react'

export default function CustomStatusBarComponent() {

  return (
    <StatusBar backgroundColor={'black'}/>
  )
}

const styles = StyleSheet.create({})